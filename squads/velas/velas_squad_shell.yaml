name: "VELAS AROMÁTICAS - SQUAD CORE"
version: "0.1-draft"
description: "Squad isolado focado no projeto de velas aromáticas."
status: "DORMANT"

# Este arquivo é apenas um esqueleto. 
# Será preenchido com as funções específicas para a página das velas quando o projeto iniciar.
roles: []
