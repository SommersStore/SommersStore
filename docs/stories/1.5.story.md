# Story 1.5 - Estabilidade do Painel e Retomada do Fluxo

**Status:** Ready for Review  
**Epic:** 1 - Estabilizacao Operacional do Control Fabric  
**Criado em:** 2026-04-16  
**Agente responsavel:** @dev (Codex)

---

## Descricao

Como operador do SommersStore, quero que o painel abra de forma confiavel e que a aba Master mantenha navegacao funcional para os modulos principais, para garantir continuidade operacional sem perder contexto entre sessoes.

---

## Criterios de Aceitacao

- [x] Bootstrap do painel aguarda servidor responder antes de abrir o navegador.
- [x] Atalho `Painel AIOX.vbs` delega para bootstrap robusto.
- [x] Navegacao de abas foi reforcada para renderizar `memory`, `funnel` e `squads`.
- [x] Validacao operacional real do ciclo startup/shutdown foi executada e registrada (checkpoints `CHK-MEM-0028` e `CHK-MEM-0029`).
- [x] `active_task` foi normalizado para acao concreta (sem placeholder `next` na sessao ativa).
- [x] Popular CONTROL com logs reais recorrentes de execucao (pulse com heartbeat e contexto).
- [ ] Avancar trilha de conteudo (ebooks) com checkpoints ativos.
- [ ] Preparar trilha de integracao de checkout.

---

## Tarefas

- [x] Corrigir ordem de inicializacao no `start_painel.bat`.
- [x] Atualizar launcher `Painel AIOX.vbs` para usar bootstrap robusto.
- [x] Ajustar roteamento de render no `switchTab` para evitar abas vazias.
- [x] Executar ciclo real de startup/pulse/shutdown e validar persistencia.
- [x] Atualizar memoria canonica (`task.md`, `memory_current_state.json`).
- [x] Registrar `session_pulse` no backend com snapshot recorrente e throttle.
- [x] Reforcar inferencia de `project_id` dos logs via sessao ativa/arquivo de sessao.

---

## File List

- `docs/aiox_dashboard.html`
- `scripts/dashboard_server.js`
- `start_painel.bat`
- `Painel AIOX.vbs`
- `docs/control/session_state.json`
- `docs/control/memory_current_state.json`
- `docs/control/memory_checkpoints.json`
- `docs/control/memory_mutations.json`
- `tests/quality/master_hub.test.cjs`
- `task.md`
