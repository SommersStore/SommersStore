# Story 2.12 - Robustez do Board de Squads e Editor

**Status:** Done  
**Epic:** 2 - MVP do Painel Unico Operacional  
**Criado em:** 2026-04-20  
**Agente responsavel:** @dev (Codex)

---

## Descricao

Como operador do painel, quero poder remover agentes de squads via drag-and-drop e abrir o editor corretamente a partir dos cards, para manter a configuracao operacional sem friccao.

---

## Criterios de Aceitacao

- [x] O board de squads possui coluna `SEM SQUAD` para desassociar agentes por drag-and-drop.
- [x] O drop em `SEM SQUAD` persiste agente sem squad no `registry.json`.
- [x] O board explica o comportamento da coluna de desassociacao.
- [x] Chamadas de `openEditorModal` na aba Memory/Squads/Agents usam `path` como primeiro parametro.
- [x] Quality gates (`lint`, `typecheck`, `test`) continuam verdes.

---

## Tarefas

- [x] Adicionar coluna virtual `SEM SQUAD` em `renderSquads`.
- [x] Normalizar `moveAgentToSquad` para converter drop em `SEM SQUAD` para `agent.squad = ''`.
- [x] Corrigir ordem de parametros em acoes de `Editar` afetadas.
- [x] Cobrir os novos comportamentos no teste de qualidade.
- [x] Executar `npm run lint`, `npm run typecheck` e `npm test`.

---

## File List

- `docs/aiox_dashboard.html`
- `tests/quality/master_hub.test.cjs`
- `docs/stories/2.12.story.md`

## Dev Agent Record

### Debug Log References

- 2026-04-28: `npm run lint`, `npm run typecheck`, `npm test`, sweep de endpoints e navegacao Puppeteer em `http://localhost:4000/`.

### Completion Notes List

- Reparei regressao do Master Hub que removia o fetch direto de `/api/project-flows`.
- Removi o inspector flutuante de clones e mantive a abertura no Inspector compacto da aba Master.
- Corrigi regressao da sidebar: Status, Control/Ops Desk, Memory, Squads, Agents, Skills e Personas voltaram a ser navegaveis.
- Corrigi regressao de Clones: Master > Sais > Clones voltou a usar `/api/personas/assets` e renderiza 6 clones com `knowledge/clones/...`.
- Reativei estado persistente de expansao das colunas de Squads e reidratacao da configuracao de agente apos refresh.
