# Story 2.67 - Verificar Botoes do Cabecalho

**Status:** Done
**Epic:** 2 - MVP do Painel Unico Operacional
**Criado em:** 2026-06-07
**Agente responsavel:** @dev (Codex)

---

## Descricao

Como operador do AIOX, quero que os botoes `Flash`, `Salvar Tudo`, `Conectar Pasta` e `IDE:4000` funcionem com feedback confiavel, para evitar copiar contexto antigo ou acionar fluxos sem clareza.

---

## Criterios de Aceitacao

- [x] `Flash` copia o contexto atual gerado na hora.
- [x] `Flash` nao prefere mais `docs/control/session_flash.txt` antigo antes de gerar o contexto vivo.
- [x] `Salvar Tudo` continua chamando `/api/cloud-sync/save-all`.
- [x] `Salvar Tudo` persiste o Flash atual em `docs/control/session_flash.txt`.
- [x] `Conectar Pasta` mantem feedback claro quando a File System Access API nao esta disponivel.
- [x] `IDE:4000` continua lendo `/api/runtime/info` e exibindo a instancia local.
- [x] Quality gates e validacao Puppeteer executados.

---

## Tarefas

- [x] Auditar handlers do cabecalho.
- [x] Corrigir `copySessionFlash` para usar `buildSessionFlash()` diretamente.
- [x] Criar helper `persistSessionFlash`.
- [x] Reutilizar `persistSessionFlash` no `Salvar Tudo`.
- [x] Atualizar testes estaticos para proteger o comportamento.
- [x] Validar cliques no painel local.
- [x] Rodar lint, typecheck e teste.

---

## Dev Agent Record

### Agent Model Used

Codex GPT-5

### Debug Log References

- Puppeteer em `http://localhost:4000/`
- `npm run lint`
- `npm run typecheck`
- `npm test`

### Completion Notes List

- O problema encontrado foi no `Flash`: ele copiava primeiro `docs/control/session_flash.txt`, que podia estar velho.
- `copySessionFlash` agora sempre gera o Flash com `buildSessionFlash()`.
- `persistSessionFlash` salva uma copia atualizada do Flash em modo best-effort.
- `Salvar Tudo` passou a reutilizar `persistSessionFlash(buildSessionFlash())`.
- Puppeteer confirmou `Flash`, `Salvar Tudo`, `Conectar Pasta` e `IDE:4000` com feedback esperado.

### File List

- `docs/stories/2.67.story.md`
- `docs/aiox_dashboard.html`
- `tests/quality/master_hub.test.cjs`

### Change Log

- 2026-06-07: Story aberta e concluida para verificar botoes do cabecalho e corrigir Flash stale.
