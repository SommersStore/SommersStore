# Story 2.1 - Quality Gates Operacionais para Master Hub

**Status:** Done  
**Epic:** 2 - MVP do Painel Unico Operacional  
**Criado em:** 2026-04-16  
**Agente responsavel:** @dev (Codex)

---

## Descricao

Como mantenedor do painel, quero quality gates reais para lint, typecheck e test, para validar o Master Hub de forma rapida e repetivel antes de seguir com novas mudancas.

---

## Criterios de Aceitacao

- [x] `npm run lint` valida sintaxe do backend e scripts inline do dashboard.
- [x] `npm run typecheck` valida consistencia estrutural de `project_flows.json`, `registry.json` e `persona_materials.json`.
- [x] `npm test` executa suite deterministica de checks do Master Hub.
- [x] `package.json` expoe scripts de qualidade funcionais (`lint`, `typecheck`, `test`).

---

## Tarefas

- [x] Criar `scripts/quality/lint.cjs` (ja existente nesta rodada e mantido como gate oficial).
- [x] Criar `scripts/quality/typecheck.cjs` com validacoes de schema e referencias.
- [x] Criar `tests/quality/master_hub.test.cjs` para checks criticos de integracao.
- [x] Atualizar `package.json` para usar os novos gates.
- [x] Executar `npm run lint`, `npm run typecheck` e `npm test` com sucesso.

---

## File List

- `scripts/quality/lint.cjs`
- `scripts/quality/typecheck.cjs`
- `tests/quality/master_hub.test.cjs`
- `package.json`
- `docs/stories/2.1.story.md`
