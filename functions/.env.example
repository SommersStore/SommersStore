# Token secreto configurado no painel Kiwify (Webhook → Token)
WEBHOOK_SECRET=cole_aqui_o_token_gerado

# API Key pública do Firebase (mesma do .env.local do frontend)
GCP_AUTH_API_KEY=cole_aqui_a_api_key
